# springboot的多环境配置: 可以选择激活哪一个配置文件
spring:
  profiles:
    active: lzy

---
# 这是cx配置吗？
server:
  port: 8080
spring:
  config:
    activate:
      on-profile: cx # 上面改成cx就行了
  #数据库连接配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tf_backend?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123
mybatis:
  type-aliases-package: com.example.domain.model
  type-handlers-package: com.example.typehandler
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 30

---
spring:
  config:
    activate:
      on-profile: lzy
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tf_lzy
    username: root
    password: tuziailuobo